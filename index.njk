---
layout: layout.njk
title: home
---

<section class="home-intro">
  <h1>안녕 🦭</h1>
  <p>this space was inspired by silly seals napping on small rocks and shit.</p>
</section>

<section class="home-featured">
  <h2>latest entries</h2>
  <div class="card-container">

    {# Journaling: filter out index.md, sort by date desc, show first #}
    {% set journalPosts = collections.journaling | reverse %}
    {% for post in journalPosts %}
      {% if "index.md" not in post.inputPath %}
        <a href="{{ post.url }}" class="post-card-link">
          <div class="post-card">
            <div class="post-title">{{ post.data.title }}</div>
            <p class="post-date">{{ post.date | readableDate }}</p>
          </div>
        </a>
        {% break %}
      {% endif %}
    {% endfor %}

    {# Learning: same logic #}
    {% set learningPosts = collections.learning | reverse %}
    {% for post in learningPosts %}
      {% if "index.md" not in post.inputPath %}
        <a href="{{ post.url }}" class="post-card-link">
          <div class="post-card">
            <div class="post-title">{{ post.data.title }}</div>
            <p class="post-date">{{ post.date | readableDate }}</p>
          </div>
        </a>
        {% break %}
      {% endif %}
    {% endfor %}
    
  </div>
</section>

<section class="home-playlist">
  <h2>🌊 for extra sillyseal vibes🌊</h2>
  <iframe 
    style="border-radius:12px" 
    src="https://open.spotify.com/embed/playlist/2ZnXnZk5ttIPWKZIJHe4Ds?utm_source=generator" 
    width="100%" height="352" frameBorder="0" allowfullscreen="" allow="autoplay; clipboard-write; 
    encrypted-media; fullscreen; picture-in-picture" loading="lazy">
  </iframe>
</section>
