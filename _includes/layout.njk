<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{{ title }}</title>
  <link rel="stylesheet" href="/styles/main.css" />
  <link rel="icon" href="/favicon.ico" type="image/x-icon" />
</head>
<body>
  <header>
    <h1><a href="/" class="site-title">sillysealthoughts</a></h1>
    <nav>
      <a href="/">home</a>
      <a href="/journaling/">journaling</a>
      <a href="/learning/">learning</a>
      <a href="/board/">board</a>
    </nav>
  </header>

  <main>
  {% if title and page.url != "/journaling/" and page.url != "/learning/" and page.url != "/" and page.url != "/board/" %}
    <h1 class="post-heading">{{ title }}</h1>
  {% endif %}

  {% if date and page.url != "/journaling/" and page.url != "/learning/" and page.url != "/" and page.url != "/board/" %}
    <p class="post-date-meta">{{ date | readableDate }}</p>
  {% endif %}

  {{ content | safe }}
</main>

  <footer>
    <p>spreading silly seal vibes 🐚</p>
  </footer>

  <!-- Add your JavaScript file -->
  <script type="module" src="/scripts/main.js"></script>
</body>
</html>
